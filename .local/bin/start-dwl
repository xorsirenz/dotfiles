#!/bin/sh

WALLPAPER_DIR="$HOME/.local/wallpapers"

dwl &
dwl_pid=$!


sleep 0.2
swaybg -i "$(find "$WALLPAPER_DIR" -type f \( -iname "*.jpg" -o -iname "*.png" -o -iname "*.jpeg" \) | shuf -n 1)" -m fill &
disown
waybar &
waybar_pid=$!

wait "$dwl_pid"

kill "$waybar_pid" 2>/dev/null
